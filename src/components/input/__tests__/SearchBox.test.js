import React from 'react';

import SearchBox from '../SearchBox';
import { exportAllDeclaration } from '@babel/types';

describe('<SearchBox />', () => {
    it.todo('should automatically invoke search function when typed');
    it.todo('should automatically invoke search function on change');
});